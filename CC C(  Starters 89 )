#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	int tc;
	cin>>tc;
	for(int i=0;i<tc;i++){
	    string s;
	    cin>>s;
	    int f=(s[0]-'0')*10+(s[1]-'0'); 
      int sec=(s[3]-'0')*10+(s[4]-'0'); 
      if(f > 12){ 
          cout<<"DD/MM/YYYY"<<endl; 
      } 
      else if(sec > 12){ 
          cout<<"MM/DD/YYYY"<<endl; 
      } 
      else{ 
          cout<<"BOTH"<<endl; 
      }
	}
	return 0;
}
