#include<bits/stdc++.h>
using namespace std;
#define ll long long int
void thrill(){
    
   // ll i=1,j=2;
  /*  ll cnt = 1; 
    ll arr[n];
    for (int i=0;i<n;i++) 
    { 
        cin>>arr[i];
        cnt = cnt * arr[i];
    } 
    cnt = n + cnt -1;
    cout<<2022 * cnt<<endl;
     */
     ll n;
     cin>>n; 
     ll mod = 1000000007;

   ll ans = n; 
    ans = (ans*((4*n-1)))%mod;
    ans = (ans* ((n+1)%mod))%mod;
    ans = (ans*166666668)%mod;
    cout<<(2022*ans)%mod<<endl;
}


int main(){
	int tc;
	cin>>tc;
	  
	for(int i=0;i<tc;i++){
thrill();
    
	}
	  return 0;
}
